/**
 * Persistence boundary for validated capability artifacts.
 *
 * Phase 3 will implement a filesystem-backed store with safe versioned
 * paths, validation-before-write, duplicate-version protection, and
 * deterministic JSON serialization.
 */
export {};
