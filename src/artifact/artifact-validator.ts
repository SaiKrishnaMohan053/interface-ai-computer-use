/**
 * Semantic validation for compiled capability artifacts.
 *
 * Responsibilities will include:
 * - unique input/output/step identifiers,
 * - valid inputRef/outputRef references,
 * - semantic target validation,
 * - success-condition validation,
 * - known-business-outcome validation,
 * - forbidden persisted-field/content checks.
 *
 * Structural Zod parsing belongs in artifact-schema.ts.
 */
export {};
