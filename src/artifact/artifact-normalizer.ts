/**
 * Deterministic normalization of successful discovery material before
 * capability compilation.
 *
 * Responsibilities will include:
 * - retaining reusable successful actions,
 * - removing failed/redundant discovery events,
 * - removing discovery-only control flow,
 * - normalizing reusable semantic targets.
 *
 * This module must never invoke an LLM.
 */
export {};
