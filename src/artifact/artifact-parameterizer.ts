/**
 * Deterministic conversion of invocation-specific discovery values into
 * typed capability input/output references.
 *
 * This module must use explicit compiler metadata or validated discovery
 * parameter metadata rather than heuristic goal parsing.
 */
export {};
