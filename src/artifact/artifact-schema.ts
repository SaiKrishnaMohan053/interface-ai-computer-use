/**
 * Zod runtime schemas for CapabilityArtifact and its nested contracts.
 *
 * Implemented incrementally beginning in Phase 3.3.
 *
 * This module owns shape validation only. Cross-reference and semantic
 * validation belong in artifact-validator.ts.
 */
export {};
