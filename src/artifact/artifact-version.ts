/**
 * Artifact schema-version and capability-version helpers.
 *
 * schemaVersion and capability identity version are intentionally separate.
 * Parsing and validation are introduced in the dedicated versioning step.
 */
export {};
