# Genuine OpenAI Discovery Success

This package demonstrates a successful genuine OpenAI-backed discovery run against the local synthetic banking application.

## Generation

Generated by the genuine discovery CLI against the live Playwright browser surface using the goal:

`Look up Alex Morgan and return their current savings balance.`

The run used model-guided discovery for observation and decision-making while browser actions remained subject to typed validation, risk classification, policy, semantic target resolution, and the surface adapter.

## Expected result

- Mode: `DISCOVERY`
- Status: `success`
- Run ID: `9635c0c9-dc3a-4b64-aa38-b1f48a359ea0`
- Verified output: `alexMorganSavingsBalance = $12,840.50`
- Human interventions: `0`
- Data classification: synthetic assignment fixture

## Important files

- `run.json` — run identity, goal, mode, and configuration metadata
- `events.jsonl` — sanitized discovery, policy, targeting, action, extraction, and completion events
- `result.json` — final verified discovery result
- `screenshots/` — synthetic browser screenshots captured during the run
- `SHA256SUMS.txt` — expected SHA-256 hashes for the frozen package

No raw OpenAI response, chain-of-thought, API key, cookie, token, credential, or browser handle is intentionally persisted.

## Integrity

`SHA256SUMS.txt` contains the expected SHA-256 hashes for the frozen files. Recompute the hashes locally and compare them with the manifest before relying on the package.
