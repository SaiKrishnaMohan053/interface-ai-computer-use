# Replay Recovery Evidence

This package demonstrates bounded deterministic recovery from a known interstitial during LLM-free replay.

## Generation

Generated by a real Playwright replay of `lookup_savings_balance` version `1.0.1` against the deterministic known-interstitial demo scenario. The artifact authorizes recovery from `KNOWN_INTERSTITIAL`; replay performs the bounded recovery without consulting an LLM.

## Expected result

```text
KNOWN_INTERSTITIAL
→ recovery attempt 1
→ known interstitial dismissed
→ recovery succeeded
→ replay continues
→ final success
```

The terminal result is:

- Status: `success`
- Steps executed: `4`
- Output: `savingsBalance = $12,840.50`

## Important files

- `run.json` — sanitized replay metadata
- `events.jsonl` — recovery start, attempt, success, replay steps, and final success verification
- `result.json` — terminal replay result
- `SHA256SUMS.txt` — expected SHA-256 hashes for the frozen package

Temporary traces and unrelated runtime artifacts are intentionally excluded.

## Integrity

`SHA256SUMS.txt` contains the expected SHA-256 hashes for the frozen files. Recompute the hashes locally and compare them with the manifest before relying on the package.
